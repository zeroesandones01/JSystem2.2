package Functions;

import java.io.IOException;

import components._JInternalFrame;

public class FncPDFViewer extends _JInternalFrame {

	/*public FncPDFViewer() throws InvalidPasswordException, IOException {
		
		File pdf_file_path = new File("C:\\PDF.PDF");
		PDDocument inputPDF = PDDocument.load(pdf_file_path);
		List<PDPage> allPages = (List<PDPage>) inputPDF.getDocumentCatalog().getPages();
		inputPDF.close();
		PDPage testPage = (PDPage)allPages.get(0);
		
		JFrame testFrame = new JFrame();
		testFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		//PDFPagePanel pdfPanel = new PDFPagePanel();
		pdfPanel.setPage(testPage);
		testFrame.add(pdfPanel);
		testFrame.setBounds(40, 40, pdfPanel.getWidth(), pdfPanel.getHeight());
		testFrame.setVisible(true);
	}*/
	
}
